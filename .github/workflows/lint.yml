   name: Brain-games

   on: [push]

   jobs:
      runs-on: ubuntu-latest
      steps:
      - name: checkout repo
        uses: actions/checkout@v2
      - name: build application
        run: make install
      - name: linting
        run: make lint